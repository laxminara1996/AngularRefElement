import { CustimizedstyleDirective } from './custimizedstyle.directive';

describe('CustimizedstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustimizedstyleDirective();
    expect(directive).toBeTruthy();
  });
});
