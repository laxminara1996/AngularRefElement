import { MyeventDirective } from './myevent.directive';

describe('MyeventDirective', () => {
  it('should create an instance', () => {
    const directive = new MyeventDirective();
    expect(directive).toBeTruthy();
  });
});
